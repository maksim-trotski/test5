<Window x:Class="_5Elem.Client.Dialogs.CategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Категория" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="Transparent">

    <Grid>
        <Border Background="{StaticResource DarkBackgroundBrush}" 
                CornerRadius="20"
                Padding="30">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding Title}" 
                           FontSize="24" 
                           FontWeight="Bold"
                           Foreground="{StaticResource PrimaryBrush}"
                           Margin="0,0,0,20"/>

                <TextBox Text="{Binding Category.Name, UpdateSourceTrigger=PropertyChanged}" 
                         Grid.Row="1"
                         Style="{StaticResource ModernTextBox}"
                         Margin="0,0,0,15"
                         Tag="Название категории"/>

                <TextBox Text="{Binding Category.Description, UpdateSourceTrigger=PropertyChanged}" 
                         Grid.Row="2"
                         Style="{StaticResource ModernTextBox}"
                         Margin="0,0,0,15"
                         Tag="Описание"
                         Height="100"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"/>

                <Button Content="Выбрать изображение"
                        Grid.Row="3"
                        Style="{StaticResource SecondaryButton}"
                        Command="{Binding SelectImageCommand}"
                        Margin="0,0,0,10"/>

                <TextBlock Text="{Binding ImageName}" 
                           Grid.Row="4"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,20"
                           TextWrapping="Wrap"/>

                <TextBlock Text="{Binding ErrorMessage}" 
                           Grid.Row="5"
                           Foreground="{StaticResource ErrorBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,10"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"/>

                <StackPanel Grid.Row="6" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right"
                            Margin="0,20,0,0">
                    <Button Content="Отмена" 
                            Style="{StaticResource SecondaryButton}"
                            Command="{Binding CancelCommand}"/>
                    <Button Content="Сохранить" 
                            Style="{StaticResource PrimaryButton}"
                            Margin="10,0,0,0"
                            Command="{Binding SaveCommand}"/>
                </StackPanel>

                <Button Content="✕"
                        Style="{StaticResource CloseButton}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="0,-20,0,0"
                        Command="{Binding CancelCommand}"/>
            </Grid>
        </Border>
    </Grid>
</Window>
